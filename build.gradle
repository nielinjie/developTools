def modelRoot = new File("./entities")




task server << {
    def dPlan = new domain.plan.Plan(modelRoot)
    def plans = [dPlan]
    new com.paic.server.Server(plans, new File('./buildSrc/src/main/webApp'), 8080).service()
}
